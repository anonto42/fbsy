# Scripts

This directory will hold installer and release helper scripts.

Planned scripts:

```text
install-service.sh
uninstall-service.sh
install-service.bat
uninstall-service.bat
```

The first Rust releases can reuse the service strategy from the Python bridge.

